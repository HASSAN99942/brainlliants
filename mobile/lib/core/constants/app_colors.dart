import 'package:flutter/material.dart';

class AppColors {
  // Background
  static const bg = Color(0xFFEEEEF5);
  static const cardSurface = Color(0xFFFFFFFF);

  // Primary palette
  static const primary = Color(0xFF3C3489);
  static const primaryLight = Color(0xFFE8E8F8);
  static const primaryMid = Color(0xFF7F77DD);

  // Text
  static const textPrimary = Color(0xFF2D2770);
  static const textSecondary = Color(0xFF8888AA);
  static const textMuted = Color(0xFFAAAAAA);

  // Action (amber CTA)
  static const action = Color(0xFFE8A020);
  static const actionDisabled = Color(0xFFF0C878);
  static const actionText = Color(0xFF2A1A00);

  // Status
  static const success = Color(0xFF1D9E75);
  static const successLight = Color(0xFFE1F5EE);
  static const error = Color(0xFFE24B4A);
  static const errorLight = Color(0xFFFCEBEB);

  // Input
  static const inputBorder = Color(0xFFE0E0F0);
  static const inputBorderFocus = Color(0xFF3C3489);

  // Bottom nav
  static const navActive = Color(0xFFE8A020);
  static const navInactive = Color(0xFF3C3489);
}
